<%= form_for(@c) do |f| %>
  <% if @c.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c.errors.count, "error") %> prohibited this c from being saved:</h2>

      <ul>
      <% @c.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
